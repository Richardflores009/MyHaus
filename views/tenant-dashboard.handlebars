{{#if property.length}}
<section>
  <h2>Your Properties</h2>
  <ol>
    {{#each property}}
    <li>
        <div>Welcome {{tenant.first_name}} {{tenant.last_name}} you are tenant</div>
        <div>property address: {{address}} property description: {{description}}</div>
        <div>tenant's first name is: {{landlord.first_name}}</div>
    </li>
    {{/each}}
  </ol>
</section>
{{/if}}
<script src="/javascript/tenant-signup"></script>