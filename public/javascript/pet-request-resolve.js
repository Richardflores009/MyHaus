// LANDLORD RESOLVES PET REQUEST (deletes from database)
const resolvePetRequest = function() {
    console.log("resolved")
};

//Event Listener for Resolve Request
document.querySelector("#resolvePetRequest").addEventListener("click", resolvePetRequest);